# ============================================================
# Master Thesis: Dating Scripts – Reproducibility Requirements
# ============================================================
# Python 3.12 | macOS Apple Silicon (MPS) / Linux (CUDA) / CPU
#
# NOTE: Several core packages (pandas, scikit-learn, bertopic,
# sentence-transformers, umap-learn, hdbscan, matplotlib, seaborn)
# were installed via conda in the original environment and are
# therefore NOT version-pinned here. Install them via conda first:
#
#   conda install pandas scikit-learn matplotlib seaborn
#   pip install bertopic umap-learn hdbscan sentence-transformers
#
# Then install the rest with:
#   pip install -r requirements.txt
# ============================================================


# ── Pinned (from original pip environment) ──────────────────

# Deep learning & training
torch==2.7.1
transformers==4.57.3
accelerate==1.10.1
peft==0.17.1
tokenizers==0.22.1
huggingface-hub==0.36.0
safetensors==0.7.0        # BERTopic model serialization (.safetensors format)

# Numerical
numpy==2.0.2
numba==0.60.0             # UMAP / HDBSCAN dependency
llvmlite==0.43.0          # numba backend

# LLM APIs
google-genai==1.47.0      # Google Gemini  (Phase 1 script mining)
openai>=1.6.0             # OpenAI GPT-4   (Phase 1 script mining) — 1.3.5 conflicts with google-genai's anyio requirement
ollama==0.6.1             # Local LLM client – gemma3 (3c notebook only)

# Utilities
tqdm==4.67.1
regex==2024.11.6


# ── Unpinned (install via pip, version flexible) ─────────────

# Topic modeling pipeline
bertopic
umap-learn
hdbscan
sentence-transformers     # default model: all-MiniLM-L6-v2

# Few-shot classification (Phase 2 script scaling)
setfit

# Reddit data cleaning
redditcleaner

# Text statistics (readability metrics in 99-visualizations_stats.ipynb)
textstat

# Document & spreadsheet export
python-docx               # .docx evaluation kit generation
openpyxl                  # .xlsx script export

# Visualization helpers (BERTopic scatter plots in 10-topic_modelling.ipynb)
adjustText                    # Non-overlapping text labels on scatter plots
scipy                         # cKDTree for emotion density calculation

# Optional – faster .jsonl parsing (recommended for large scraped datasets)
orjson
